const { response } = require('express');

describe("Funcionalidades de Login", () => {

    beforeAll(() => { 
        require('dotenv').config();
    });

    it("Login Usuario",() =>{
        expect(true).toBeTrue();
    } )
    it("Login Admin",() =>{
        expect(true).toBeTrue();
    } )
    it("Olvidar Contrasenia",() =>{
        expect(true).toBeTrue();
    } )
    it("PreRegistro",() =>{
        expect(true).toBeTrue();
    } )
    it("Crear Usuario PosRegistro",() =>{
        expect(true).toBeTrue();
    } )
})
