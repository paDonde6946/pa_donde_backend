const { response } = require('express');

describe("Funcionalidades de Notificaciones", () => {

    beforeAll(() => { 
        require('dotenv').config();
    });

    it("Envio Notificaciones Vehiculo", async() => {
        expect(true).toBeTrue();
    })


})
