const { response } = require('express');

describe("Funcionalidades de Mensaje", () => {

    beforeAll(() => { 
        require('dotenv').config();
    });

    it("Agregar Mensaje", async() => {
        expect(true).toBeTrue();
    })

    
    it("Traer Conversacion", async() => {
        expect(true).toBeTrue();
    })

})
