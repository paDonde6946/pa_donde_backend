const { response } = require('express');

describe("Funcionalidades de Dashboard", () => {

    beforeAll(() => { 
        require('dotenv').config();
    });

    it("Cantidad Usuarios", () => {
        expect(true).toBeTrue();
    })

    it("Cantidad Vehiculos", () => {
        expect(true).toBeTrue();
    })

    it("Cantidad Conductores", () => {
        expect(true).toBeTrue();
    })

    it("Cantidad Servicios", () => {
        expect(true).toBeTrue();
    })

    it("Conductores Mas Servicios", () => {
        expect(true).toBeTrue();
    })

    it("Cantidad Carros Motos", () => {
        expect(true).toBeTrue();
    })

})
